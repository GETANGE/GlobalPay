name: api-gateway-workflow

on:
    push:
        branches:
            - gateway
    pull_request:
        types: [opened, closed, auto_merge_enabled]
    workflow_dispatch:

jobs:
    getCode:
        runs-on: Ubuntu-latest