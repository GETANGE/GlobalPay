name: Transaction-workflow

on:
    push:
        branches:
            - transaction
        paths:
            - '**'
    pull_request:
        types: [opened, closed, auto_merge_enabled]
    workflow_dispatch:

jobs:
    getCode:
        runs-on: Ubuntu-latest
        steps:
            - name: First step
              run: echo "transaction-workflow"