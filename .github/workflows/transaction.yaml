name: Transaction-workflow

on:
    push:
        branches:
            - transaction
        paths:
            - '**'
    workflow_dispatch:

jobs:
    getCode:
        runs-on: Ubuntu-latest
        steps:
            - name: First step
              run: echo "transaction-workflow"