name: payment-workflow

on:
    push:
        branches:
            - payment
        paths:
            - '**'
    workflow_dispatch:

jobs:
    getCode:
        runs-on: Ubuntu-latest
        steps:
            - name: First step
              run: echo "payment-workflow"