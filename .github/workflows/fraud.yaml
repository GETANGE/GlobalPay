name: Fraud-workflow

on:
    push:
        branches:
            - fraud
    pull_request:
        types: [opened, closed, auto_merge_enabled]
    workflow_dispatch:

jobs:
    getCode:
        runs-on: Ubuntu-latest
        steps:
            - name: First step
              run: echo "Fraud-workflow"