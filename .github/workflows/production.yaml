name: Deploy-to-VPS-workflow

on:
    push:
        branches:
            - master
    pull_request:
        types: [opened, closed, auto_merge_enabled]
    workflow_dispatch:

jobs:
    getCode:
        runs-on: Ubuntu-latest